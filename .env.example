# Copy to .env.local and fill in. Never commit the real file.
# Read by api/chat.ts through `vercel dev` locally, and set as a Sensitive
# variable in the Vercel project for Production.
OPENAI_API_KEY=
